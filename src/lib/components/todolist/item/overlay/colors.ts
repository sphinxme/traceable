/**
 * 任务状态颜色映射 — 用于 Handle 组件的状态圆点显示。
 *
 * - `TODO` — 灰色（`bg-zinc-500`）：待处理
 * - `BLOCK` — 蓝色（`bg-blue-300`）：阻塞
 * - `DONE` — 浅灰（`bg-zinc-100`）：已完成
 */
export const colors = {
    "TODO": "bg-zinc-500",
    "BLOCK": "bg-blue-300",
    "DONE": "bg-zinc-100",
}